

<% @postsr = Post.last(10).reverse %>
<% @postsr.each do |t| %>

<div class='row posts'>
	<div class='col-4'></div>
	<div class='col-4 postStuff'>
<h1><%=t.title%></h1>
<p><%=t.content%></p>
</div>
</div>

<%end%>
